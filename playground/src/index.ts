/*
  Built-in Types P2

*/

// if we initialize a variable with value > TS knows the type of that variable > we don't need to annotate > inference > hover on the variable > type = number
let sales = 123_456_758

let course: string = 'Typescript'
let isPublished: boolean = true

// type = any > since we did not initialize variable
let level
